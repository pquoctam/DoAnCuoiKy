@model dynamic
<div class="swiper-slide combo-card">
    <img src="@Model.ImageUrl" />
    <h3>@Model.Title</h3>
    <p class="price">
        Giá: <span class="current">@Model.CurrentPrice</span>
        <span class="original">@Model.OriginalPrice</span>
    </p>
    <button>XEM CHI TIẾT</button>
</div>
